# Klawforge — Public Release Channel

Signed + notarized macOS builds and the Tauri auto-update feed (`latest.json`) for [Klawforge](https://klawforge.app).

Source code lives in a private repository; this repo only hosts release artifacts.
